[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=46.0.5",
    "flask>=3.1.3",
    "gunicorn>=25.1.0",
    "requests>=2.32.5",
]
